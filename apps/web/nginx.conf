server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA: qualquer rota desconhecida (ex.: /indicators/usd-brl) cai no index.html
  # e o react-router assume o roteamento no cliente.
  location / {
    try_files $uri /index.html;
  }
}
